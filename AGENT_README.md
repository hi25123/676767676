# Agent scaffold

This repository now includes a minimal agent in `agent.js`.

- Run: `node agent.js`
- Type input and press enter
- The agent echoes responses

Customize `respond()` for your own business logic.
